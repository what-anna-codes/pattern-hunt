import { Colors, Fills, Shapes, Counts } from "../ts/types";

export const cardVariants = {
  color: Object.values(Colors),
  shape: Object.values(Shapes),
  fill: Object.values(Fills),
  count: Object.values(Counts),
};

export const PENALTY = 10;
